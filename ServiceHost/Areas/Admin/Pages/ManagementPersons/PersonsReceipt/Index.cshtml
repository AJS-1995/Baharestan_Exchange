@page
@model ServiceHost.Areas.Admin.Pages.PersonsReceipt.IndexModel
@using Configuration.Permissions.General;
@using _0_Framework.Application;
@inject _0_Framework.Application.Auth.IAuthHelper AuthHelper;
@{
    Layout = "Shared/_AdminLayout";
    ViewData["title"] = "مدیریت رسید و برد افراد";
    var currentAccout = AuthHelper.CurrentUserInfo();
}
@if (Model?.permissionQueryModels?.ListGeneral == GeneralPermissions.ListGeneral || Model?.permissionQueryModels?.AdminGeneral == GeneralPermissions.AdminGeneral)
{
    <section class="content">
        <div class="row">
            <div class="col-sm-12">
                <div class="card">
                    <div class="card-header d-flex justify-content-between bb-1 border-info">
                        <div class="header-title">
                            @if (Model?.permissionQueryModels?.AddGeneral == GeneralPermissions.AddGeneral || Model?.permissionQueryModels?.AdminGeneral == GeneralPermissions.AdminGeneral)
                            {
                                <a class="btn btn-sm btn-icon btn-outline-success mx-2" title="ثبت جدید" href="#showmodal=@Url.Page("./Index", "Create")"><i class="fa-solid fa-plus fa-lg"></i></a>
                            }
                            @if (Model?.permissionQueryModels?.RemovedGeneral == GeneralPermissions.RemovedGeneral || Model?.permissionQueryModels?.AdminGeneral == GeneralPermissions.AdminGeneral)
                            {
                                <a class="btn btn-sm btn-icon btn-outline-danger mx-2" title="حذف شده ها" href="#showmodal=@Url.Page("./Index", "Removed")"><i class="fa-solid fa-trash-can fa-lg"></i></a>
                            }
                            @if (Model?.permissionQueryModels?.ActivedGeneral == GeneralPermissions.ActivedGeneral || Model?.permissionQueryModels?.AdminGeneral == GeneralPermissions.AdminGeneral)
                            {
                                <a class="btn btn-sm btn-icon btn-outline-warning mx-2" title="غیرفعال ها" href="#showmodal=@Url.Page("./Index", "Actived")"><i class="fa-solid fa-toggle-off fa-lg"></i></a>
                            }
                            <a class="btn btn-sm btn-icon btn-outline-info mx-2" title="برگشت" asp-page="/Index"><i class="fa-solid fa-left-long fa-lg"></i></a>
                            <span class="card-title mx-2">لیست رسید و برد افراد</span>
                        </div>
                    </div>
                    <div class="card-body">
                        <div class="table-responsive">
                            <table id="example" class="table table-bordered table-hover display nowrap margin-top-10 w-p100 js-serial">
                                <thead class="table-Primary">
                                    <tr>
                                        <th class="text-center">نام</th>
                                        <th class="text-center">شماره تماس</th>
                                        <th class="text-center">آدرس</th>
                                        <th class="text-center">کمپنی</th>
                                        @if (Model.idAgencies == 0)
                                        {
                                            <th class="text-center">نماینده گی</th>
                                        }
                                        <th class="text-center">عملیات</th>
                                    </tr>
                                </thead>
                                <tbody id="tbody">
                                    @foreach (var item in Model.Persons)
                                    {
                                        <tr>
                                            <td class="text-center">@item.Name</td>
                                            <td class="text-center" dir="ltr">@item.Mobile</td>
                                            <td class="text-center">@item.Address</td>
                                            <td class="text-center">@item.Company</td>
                                            @if (Model.idAgencies == 0)
                                            {
                                                if (item.AgenciesId == 0)
                                                {
                                                    <td class="text-center">مرکزی</td>
                                                }
                                                else
                                                {
                                                    <td class="text-center">@item.AgenciesName</td>
                                                }
                                            }
                                            <td class="text-center">
                                                <a class="btn btn-sm btn-icon btn-outline-primary" title="حسابات" asp-page="./Details" asp-route-persons_id="@item.Id"><i class="fa-solid fa-search fa-lg"></i></a>
                                            </td>
                                        </tr>
                                    }
                                </tbody>
                            </table>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </section>
}
@section Scripts
{
    <script src="~/Theme/assets/formatter/formatter.js"></script>
    <script src="~/Theme/assets/formatter/jquery.formatter.js"></script>
    <script src="~/Theme/material-rtl/js/formatter.js"></script>
    <script src="~/Theme/assets/datatable/datatables.js"></script>
    <script src="~/Theme/material-rtl/js/data-table.js"></script>
    <script>
        function addRowCount(tableAttr) {
            $(tableAttr).each(function () {
                $('th:first-child, thead td:first-child', this).each(function () {
                    var tag = $(this).prop('tagName');
                    $(this).before('<' + tag + ' class="text-center" >#</' + tag + '>');
                });
                $('td:first-child', this).each(function (i) {
                    $(this).before('<td class="text-center">' + (i + 1) + '</td>');
                });
            });
        }
        addRowCount('.js-serial');
    </script>
    <script>
        function agencies(agenciesid) {
            if (agenciesid != "") {
                var postData = {
                    'agenciesid': agenciesid,
                };
                $.ajax({
                    ccontentType: 'application/x-www-form-urlencoded',
                    dataType: 'json',
                    type: "GET",
                    url: "@Url.Page("./Index", "Person")",
                    data: postData,
                    success: function (date) {
                        if (date.length > 0) {
                            $('#PersonId').empty();
                            var option = '';
                            option += '<option value="">افراد ...</option>';
                            for (var i = 0; i < date.length; i++) {
                                option += '<option value="' + date[i].id + '">' + date[i].name + '</option>';
                            }
                            $('#PersonId').append(option);
                        }
                    },
                });
                $.ajax({
                    ccontentType: 'application/x-www-form-urlencoded',
                    dataType: 'json',
                    type: "GET",
                    url: "@Url.Page("./Index", "SafeBox")",
                    data: postData,
                    success: function (date) {
                        if (date.length > 0) {
                            $('#SafeBoxId').empty();
                            var option = '';
                            option += '<option value="">خزانه ...</option>';
                            for (var i = 0; i < date.length; i++) {
                                option += '<option value="' + date[i].id + '">' + date[i].name + '</option>';
                            }
                            $('#SafeBoxId').append(option);
                        }
                    },
                });
            }
        }
    </script>
    <script>
        function InActive(id) {
            Swal.fire({
                title: 'آیا میخواهید رسید و برد افراد غیر فعال شود ؟',
                text: "پس میتوانید فعال کنید !",
                icon: 'warning',
                showCancelButton: true,
                confirmButtonColor: '#3085d6',
                cancelButtonColor: '#d33',
                cancelButtonText: 'خیر',
                confirmButtonText: 'بلی'
            }).then((result) => {
                if (result.isConfirmed) {
                    var postData = {
                        'id': id,
                    };
                    $.ajax({
                        ccontentType: 'application/x-www-form-urlencoded',
                        dataType: 'json',
                        type: "GET",
                        url: "@Url.Page("./Index", "InActive")",
                        data: postData,
                        success: function (date) {
                            if (date.isSuccedded == true) {
                                Swal.fire({
                                    position: 'center',
                                    icon: 'success',
                                    title: date.message,
                                    showConfirmButton: false,
                                    timer: 2000
                                });
                                setTimeout(function () {
                                    window.location.reload(1);
                                }, 2500);
                            } else {
                                Swal.fire({
                                    position: 'center',
                                    icon: 'warning',
                                    title: date.message,
                                    showConfirmButton: false,
                                    timer: 2000
                                });
                            }
                        },
                    });
                }
            })
        }

        function Active(id) {
            Swal.fire({
                title: 'آیا میخواهید رسید و برد افراد فعال شود ؟',
                text: "پس میتوانید غیر فعال کنید !",
                icon: 'warning',
                showCancelButton: true,
                confirmButtonColor: '#3085d6',
                cancelButtonColor: '#d33',
                cancelButtonText: 'خیر',
                confirmButtonText: 'بلی'
            }).then((result) => {
                if (result.isConfirmed) {
                    var postData = {
                        'id': id,
                    };
                    $.ajax({
                        ccontentType: 'application/x-www-form-urlencoded',
                        dataType: 'json',
                        type: "GET",
                        url: "@Url.Page("./Index", "Active")",
                        data: postData,
                        success: function (date) {
                            if (date.isSuccedded == true) {
                                Swal.fire({
                                    position: 'center',
                                    icon: 'success',
                                    title: date.message,
                                    showConfirmButton: false,
                                    timer: 2000
                                });
                                setTimeout(function () {
                                    window.location.reload(1);
                                }, 2500);
                            } else {
                                Swal.fire({
                                    position: 'center',
                                    icon: 'warning',
                                    title: date.message,
                                    showConfirmButton: false,
                                    timer: 2000
                                });
                            }
                        },
                    });
                }
            })
        }

        function Remove(id) {
            Swal.fire({
                title: 'آیا میخواهید رسید و برد افراد حذف شود ؟',
                text: "پس میتوانید بازیابی کنید !",
                icon: 'warning',
                showCancelButton: true,
                confirmButtonColor: '#3085d6',
                cancelButtonColor: '#d33',
                cancelButtonText: 'خیر',
                confirmButtonText: 'بلی'
            }).then((result) => {
                if (result.isConfirmed) {
                    var postData = {
                        'id': id,
                    };
                    $.ajax({
                        ccontentType: 'application/x-www-form-urlencoded',
                        dataType: 'json',
                        type: "GET",
                        url: "@Url.Page("./Index", "Remove")",
                        data: postData,
                        success: function (date) {
                            if (date.isSuccedded == true) {
                                Swal.fire({
                                    position: 'center',
                                    icon: 'success',
                                    title: date.message,
                                    showConfirmButton: false,
                                    timer: 2000
                                });
                                setTimeout(function () {
                                    window.location.reload(1);
                                }, 2500);
                            } else {
                                Swal.fire({
                                    position: 'center',
                                    icon: 'warning',
                                    title: date.message,
                                    showConfirmButton: false,
                                    timer: 2000
                                });
                            }
                        },
                    });
                }
            })
        }

        function Reset(id) {
            Swal.fire({
                title: 'آیا میخواهید رسید و برد افراد بازیابی شود ؟',
                text: "پس میتوانید حذف کنید !",
                icon: 'warning',
                showCancelButton: true,
                confirmButtonColor: '#3085d6',
                cancelButtonColor: '#d33',
                cancelButtonText: 'خیر',
                confirmButtonText: 'بلی'
            }).then((result) => {
                if (result.isConfirmed) {
                    var postData = {
                        'id': id,
                    };
                    $.ajax({
                        ccontentType: 'application/x-www-form-urlencoded',
                        dataType: 'json',
                        type: "GET",
                        url: "@Url.Page("./Index", "Reset")",
                        data: postData,
                        success: function (date) {
                            if (date.isSuccedded == true) {
                                Swal.fire({
                                    position: 'center',
                                    icon: 'success',
                                    title: date.message,
                                    showConfirmButton: false,
                                    timer: 2000
                                });
                                setTimeout(function () {
                                    window.location.reload(1);
                                }, 2500);
                            } else {
                                Swal.fire({
                                    position: 'center',
                                    icon: 'warning',
                                    title: date.message,
                                    showConfirmButton: false,
                                    timer: 2000
                                });
                            }
                        },
                    });
                }
            })
        }

        function Delete(id) {
            Swal.fire({
                title: 'آیا میخواهید رسید و برد افراد کامل حذف شود ؟',
                text: "پس نمیتوانید بازیابی کنید !",
                icon: 'warning',
                showCancelButton: true,
                confirmButtonColor: '#3085d6',
                cancelButtonColor: '#d33',
                cancelButtonText: 'خیر',
                confirmButtonText: 'بلی'
            }).then((result) => {
                if (result.isConfirmed) {
                    var postData = {
                        'id': id,
                    };
                    $.ajax({
                        ccontentType: 'application/x-www-form-urlencoded',
                        dataType: 'json',
                        type: "GET",
                        url: "@Url.Page("./Index", "Delete")",
                        data: postData,
                        success: function (date) {
                            if (date.isSuccedded == true) {
                                Swal.fire({
                                    position: 'center',
                                    icon: 'success',
                                    title: date.message,
                                    showConfirmButton: false,
                                    timer: 2000
                                });
                                setTimeout(function () {
                                    window.location.reload(1);
                                }, 2500);
                            } else {
                                Swal.fire({
                                    position: 'center',
                                    icon: 'warning',
                                    title: date.message,
                                    showConfirmButton: false,
                                    timer: 2000
                                });
                            }
                        },
                    });
                }
            })
        }
    </script>
}