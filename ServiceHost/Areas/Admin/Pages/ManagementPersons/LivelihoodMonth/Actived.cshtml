@model Contracts.ManagementPresonsContracts.LivelihoodMonthContracts.LivelihoodMonthRemoved
@using _0_Framework.Application;
@{
    ViewData["title"] = "لیست معاش کارمند غیرفعال شده";
}
<div class="modal-header">
    <a class="btn btn-sm btn-icon btn-outline-warning" asp-page="/Admin/ManagementPersons/PersonsReceipt/Index" asp-route-personsId="@Model.PersonsId"><i class="fa-solid fa-close fa-lg"></i></a>
    <h4 class="modal-title">لیست معاش کارمند غیرفعال شده</h4>
</div>

<div class="modal-body">
    <div class="table-responsive">
        <table class="table table-bordered table-hover display nowrap margin-top-10 w-p100">
            <thead class="table-Primary">
                <tr>
                    <th class="text-center">#</th>
                    <th class="text-center">سال</th>
                    <th class="text-center">ماه</th>
                    <th class="text-center">مقدار</th>
                    <th class="text-center">عملیات</th>
                </tr>
            </thead>
            <tbody id="tbodyv">
                @foreach (var item in Model.LivelihoodMonthRemoveds)
                {
                    <tr>
                        <td class="text-center"></td>
                        <td class="text-center">@item.Year</td>
                        <td class="text-center">@item.Month.ToMonth() <sub>(@item.Month)</sub></td>
                        <td class="text-center">@item.Amount.ToMoney() <sub>(@item.MoneyName)</sub></td>
                        <td class="text-center">
                            <a class="btn btn-sm btn-icon btn-outline-danger" title="فعال کردن" onclick="Active(@item.Id)"><i class="fa-solid fa-check fa-lg"></i></a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>